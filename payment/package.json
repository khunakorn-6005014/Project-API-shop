{
  "name": "payment",
  "version": "1.0.0",
  "description": "docker build -t payment-service ./payment\r docker run -d -p 3001:3001 payment-service\r # List running containers: \r docker ps\r ## List all containers (including stopped ones): \r docker ps -a\r ## Stop a container: \r docker stop <container_id>\r ## Remove a container: \r docker rm <container_id>",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "express-async-handler": "^1.2.0",
    "jsonwebtoken": "^9.0.2",
    "kafkajs": "^2.2.4",
    "mongoose": "^8.15.1",
    "uuid": "^11.1.0",
    "auth-lib": "file:/shared/auth-lib"
  }
}
